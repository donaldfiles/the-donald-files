# Supabase (anon key is public client-safe; never commit service role keys)
VITE_SUPABASE_URL=https://rkudbltzydukizjtjonl.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Resend — server-side only (Edge Function / API). Do NOT use VITE_ prefix.
# Never expose this key in the browser bundle.
RESEND_API_KEY=re_xxxxxxxx
RESEND_FROM_EMAIL=noreply@example.com
